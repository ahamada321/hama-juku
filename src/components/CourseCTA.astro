---
interface Props {
  title: string;
  description: string;
  url: string;
  salePrice: number;
  price: number;
}

const { title, description, url, salePrice, price } = Astro.props;
---

<section id="course-cta" class="relative bg-white/70 backdrop-blur-md py-8 md:py-10">
  <div class="lg:max-w-7xl mx-auto px-6 relative text-center">
    <h2 class="text-3xl md:text-4xl font-semibold text-black mb-3">{title}</h2>
    <p class="text-lg text-gray-600 max-w-2xl mx-auto mb-6">{description}</p>
    
    <div class="rounded-2xl bg-white p-6 border border-black/10 max-w-sm mx-auto">
      <div class="flex justify-center items-baseline mb-4 gap-4">
        <h3 class="text-3xl font-semibold text-black">¥ {new Intl.NumberFormat('ja-JP').format(salePrice)}</h3>
        <p class="text-gray-500">| <del>¥{new Intl.NumberFormat('ja-JP').format(price)}</del></p>
      </div>
      <div class="flex flex-col text-center gap-4">
        <a
          href={url}
          class="nav-link py-4 rounded-xl font-semibold border-2 border-black/10 shadow-sm text-gray-800 transition-all transform bg-lime-300 hover:bg-lime-400"
        >カートに入れる</a>
        <a
          href={`${url}?et=free_trial`}
          class="nav-link py-4 rounded-xl font-semibold border-2 border-black/10 shadow-sm text-gray-800 transition-all transform bg-white hover:bg-gray-200"
        >無料プレビューを見る</a>
      </div>
    </div>

  </div>
</section>